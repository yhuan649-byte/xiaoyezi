# 夏日慢车 · 小叶子 2.5D 展示页

这是独立的图片展示网页。代码、样式、感应和图层处理全部在 index.html 内，不依赖旧版 app.js、style.css、effects.js，不用安装开发工具。原网站的陀螺仪、摇一摇、下拉返回交互保留在这版中。

## 手机直接上传

1. 下载 leaf-2-5d.zip，在手机文件管理器中解压。
2. 在 GitHub 网站打开你的仓库，选择 Add file → Upload files。按钮找不到时切换手机浏览器“桌面版网站”。
3. 把解压后的 index.html 和 8 张 JPG 一起上传到仓库根目录并提交。它们必须在同一个目录。不要只上传 ZIP，也不要把 index.html 放一层、图片放另一层。
4. 如果已有旧版 index.html，用这次的覆盖；如希望保留旧网站，把整套文件放进单独子文件夹（例如 train），通过该文件夹网址访问。
5. 沿用原 GitHub Pages 设置即可。新仓库在 Settings → Pages，选择 Deploy from a branch → main → / (root) → Save。
6. 等待部署完成，打开 Pages 给出的网站地址。首次加载结束后，点击“开启陀螺仪”，按浏览器提示允许权限。

你自己的图片不需要重新命名，代码逐字使用下表中的原上传文件名。压缩包也已放入这些名字的原图，图片内容没有被改写。

## 图片与图层对应表

| 图层 | 用途 | 原文件名 |
| --- | --- | --- |
| L0 | 天空远景 | ResizedImage_2026-09-19_00-24-23_2626[1].jpg |
| L1 | 窗外树影 | ResizedImage_2026-09-19_00-32-56_5264[1].jpg |
| L2 | 车窗车厢 | IMG_20260919_003553.jpg |
| L3 | 白猫 | ResizedImage_2026-09-19_00-29-25_8440[1].jpg |
| L4 | 小叶子主体 | ResizedImage_2026-09-19_00-39-38_4648[3].jpg |
| L5 | 独立头发 | ResizedImage_2026-09-19_00-39-38_5440[4].jpg |
| L6 | 前景光斑 | ResizedImage_2026-09-19_00-39-37_4369[1].jpg |
| L7 | 前景树叶 | ResizedImage_2026-09-19_00-39-38_7391[2].jpg |

file_00000000451482469a1099e855df4291.png 是图层拆分参考图，已原样附在包中；网页不使用这张图，一起上传也不影响运行。deployment.md 是本说明，不参与网页运行。

## 使用方法

- 开启陀螺仪：首次收到有效数据时，以当前握姿为正面。天空移动最慢，树叶和光斑移动更明显，人物和车窗处在中间。
- 视角归中：重新校准到你当前的握姿。支持手机横竖屏方向变换。
- 摇一摇：在已开启感应的情况下，切换“晨光微风 / 午后金叶 / 薄荷晴空”。首次采样不触发，连续摇动有 2.2 秒冷却。
- 无感应时：在图片上左右拖动，也可以用鼠标或聚焦画面后按方向键操作。普通页面保留纵向滚动和双指缩放。
- 沉浸欣赏：点右上角 ⛶。退出时点 ×、按 Esc，或从顶部“下拉返回”手柄向下拉超过约 85px。
- 暂停动态：停止视差、微动和摇一摇；仍可手动切换光景。系统开启“减少动态效果”时默认暂停，可以手动继续。
- 画面与感应设置：调景深强度、黑底边缘柔化，单独开关发梢、树叶和摇一摇。所有设置是本次页面会话设置。

## 本次素材的实际情况

8 张图都是 JPG，其中 L3/L4/L5/L6/L7 的背景是实心黑色，并非透明通道。网页在运行时用 Canvas 生成近黑背景遮罩；光斑采用滤色叠加，原 JPG 文件不变。JPG 的压缩和已混入黑色的发丝边缘无法完全等同于原生透明 PNG，可以在设置中调整边缘柔化。

L2 含有窗外风景，程序按本图的窗口位置生成遮罩，露出独立移动的 L0/L1。L1 对中央天空渐隐，避免完全盖住 L0。L4 自带完整头发，而 L5 是另一张单独构图的头发，因此只把 L5 的局部发梢放在人物后方做微动，不是完整头发换装或骨骼动画。

这是多层位移与轻微透视构成的 2.5D 场景，不是可任意绕到人物背面的真实 3D 模型。竖屏和横屏分别调整构图；原图边缘已经截断的部分不会凭空恢复。

## 感应或图片不显示时

- 在 GitHub Pages 的 HTTPS 页面测试。不要把手机文件管理器直接打开 HTML 的结果当作部署结果；本地 file:// 的感应和 Canvas 读像素可能受浏览器限制。
- 感应需手机和浏览器支持。不支持、拒绝权限或暂无数据时会显示提示，不会伪装成已经连接。App 内置浏览器可能不提供传感器，请用系统浏览器打开。
- 在“画面与感应设置”里查看 8 个素材的载入状态，失败项会列出完整文件名。
- 检查大小写、方括号、数字、扩展名是否完全一致。网页不会去匹配改名后的副本，例如多出 (1) 的下载文件。
- 上传后先等待部署结束再刷新；更新后浏览器仍显示旧版时，清除该站点缓存再试。

## 验证范围

已检查完整文件名与原图字节一致性、脚本语法、8 层素材处理；使用同一份画布代码渲染了横屏、竖屏与沉浸构图；用模拟事件检查了陀螺仪基准、摇一摇首次采样防误触和冷却、暂停状态。

当前预览浏览器不允许打开本地项目，未做完整浏览器布局测试，也未做真实手机传感器实测。
